Place optional trained models here in future versions.

Suggested model interfaces:
- document_classifier.onnx
- tamper_detector.onnx
- layout_detector.onnx

The current final demo intentionally uses lightweight OpenCV/Python methods so it runs on a normal student laptop without GPU requirements.
